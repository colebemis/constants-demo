import system from 'system-components';

const Code = system({
   is: 'code',
   font: 'Menlo, monospace',
   fontSize: 1,
   lineHeight: 'copy',
   color: 'grayAlpha.9',
});

Code.displayName = 'Code';

export default Code;
